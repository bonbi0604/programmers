#include <iostream>
#include <algorithm>
#include <vector>
#include <string>
#include <set>
#include <math.h>
using namespace std;

int main() {

        cin.tie(NULL);
        cout.tie(NULL);
        
        int n;
        cin >> n;

        cout << int(sqrt(n));

        return 0;
}